
spa(single page application) - сайт или приложение использующее единственный html-файл в качестве обёртки и динамически загружающий с сервера html, css, js.

component (компонент) - функция (функциональный компонент) принимающая параметр props и возвращающая разметку jsx, а также component является тэгом. (<component />)
Также компонент - это class, который наследуется от React.component и содержит как минимум метод render. Также, как и ФК возвращает jsx разметку.

Props представляет (объект) коллекцию значений, которые ассоциированы с компонентом. Эти значения позволяют создавать динамические компоненты, которые не зависят от жестко закодированных статических данных.

Чистая функция это функция, которая не имеет побочных эффектов и всегда возвращает одинаковый результат при одних и тех же аргументах.
( примеры побочных эффектов : Видоизменение входных параметров - console.log - HTTP вызовы (AJAX/fetch) - Изменение в файловой системе - Запросы DOM)

actionCreator - функция, возвращающая экшн. Экшн - это объект, имеющий как минимум свойство type.

reducer - чистая функция, которая принимает стейт и экшн и возвращает новый стейт, усли стейт изменился (по правилам иммутабельности, то есть работая с копией) или "старый" стейт, если ничего не менялосью

Интерфейс функции (объекта) - это способ взаимодействия с функцией (объектом). (вызов, передача аргументов и тд).

Основные типы запросов на сервер: get/post/put/delete/patch
get - получение данных
post - отправка данных
put - обновление 
delete - удаление
patch - частичное изменение